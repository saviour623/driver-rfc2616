#include <iostream>
#include <stdint.h>

using namespace std;

int main(void)
{
	uint32_t mask{0x1010101ul};
	uint32_t where;

	// while (mask)
	{
		where = __builtin_ctz(mask >> 7);
		cout << where << endl;
		mask ^= (1ull << (where + 7));
	}
}